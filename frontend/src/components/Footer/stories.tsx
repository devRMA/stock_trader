import { Meta, StoryFn } from '@storybook/react/types-6-0';

import Footer from '.';

export default {
    title: 'Footer',
    component: Footer,
} as Meta;

export const Basic: StoryFn = () => <Footer />;
